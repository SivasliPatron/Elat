/* =============================================================================
   Extracted Inline JavaScript from ueber-uns.html
   Date: 2025-09-27
   Purpose: AOS initialization that was inline
============================================================================= */

// AOS Animationen initialisieren
document.addEventListener('DOMContentLoaded', function() {
    // AOS initialisieren
    AOS.init({
        duration: 800,
        easing: 'ease-in-out',
        once: true,
        mirror: false
    });
});
